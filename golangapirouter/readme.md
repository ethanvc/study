# 概述
在软件系统中，为了屏蔽单点故障，尝尝需要有一定的冗余措施。为了实现机房冗余，设计了如下的请求路由方案。该方案一般应用到系统中名为gateway的服务中。
由于机房存在搬迁、裁撤的需求，抽象出一个单元的概念，单元对应一组服务，部署在同一个机房，每一个请求路由到合适的单元进行处理。
当单元所在的机房需要切换时，将流量全部切到其它单元，然后执行机房搬迁逻辑，完成之后，切回流量即可。

# 需求列表
1. 正常都路由到unit0，提供完整服务，unit0流量全部切走的时候，路由到unit1，提供有有损服务。
2. 一个api，如何根据参数计算出一个值，然后根据这个值和流量比例，选择一个unit。
   1. 可以选user_id、商户订单号，他们的区别是？
3. 不同环境下，去除参数的前缀的长度不同，如何支持？可以通过condition对环境值做判断。

# 行业调研
## 蚂蚁集团单元化架构
https://mp.weixin.qq.com/s/MT-Pe2WCsC4kJHY_qeoAuA

## 存储层多活
TDSQL
## 阿里 MSHA异地多活架构
关键技术：
RZone (路由规则引擎)
  ├─ 路由键提取 (类似 Preprocessor)
  ├─ 规则匹配 (类似 Strategy)
  └─ 单元选择 (类似 Unit ID)

MSHA (多活容灾平台)
  ├─ 流量调度 (类似 DR Platform)
  ├─ 数据同步
  └─ 切流验证
参考资料：
- [MSHA实现异地双活](https://help.aliyun.com/document_detail/2929420.html)
- [多活容灾MSHA产品介绍](https://apsara-doc.oss-cn-hangzhou.aliyuncs.com/apsara-pdf/enterprise/v_3_16_0_20220117/msha/zh/enterprise-product-introduction.pdf)
  - 同城机房物理距离通常 < 50km，跨机房的网络延迟较小（RT < 2 ms）

## 饿了么异地多活技术实现（二）API-Router的设计与实现

- [饿了么异地多活技术实现（二）API-Router的设计与实现](https://zhuanlan.zhihu.com/p/32587960)
## 字节跳动 - ByteArch（多活架构）


# 需求分析
**多活的本质是解决数据的可用性问题**。
数据的可用性归纳为解决如下问题：
1. 不能容忍数据不一致的，只能去主所在的地方处理。
2. 可以容忍数据不一致的，优先就近处理。
3. 有损容忍数据不一致的，优先去主所在的地方处理。
4. 为了降低数据不一致带来的负面影响，可以通过各种缓存处理措施，加快数据一致性进度。

当gateway收到一个请求之后，需要判断，该请求应该转发到后端哪一个单元，存在如下场景：
1. fix：请求需要指定的单元进行处理，且gateway能够判断具体的单元编号。
2. any：请求在任一单元可以处理。
3. try：请求需要指定的单元进行处理，但是gateway无法轻易判断是哪个单元，因此需要先尝试请求一次，根据后端的返回，判断是否已经处理，或者是后端提供了正确的处理单元编号。

对于不支持单元化的服务，可能有类似主备服务，api需要根据特性路由到主服务还是备服务。一般写接口和要求数据一致性的接口需要路由到主服务，否则路由到备服务也能处理。

任何一个api的路由，最终都可以分类到上述三种场景。以下举例说明。

## 无状态接口
如果请求无状态，或者是容忍缓存不一致，任一单元都可以处理该请求，则属于any路由方式。gateway采用一定的策略选择一个单元即可处理请求。
## 收单
收单存在两种场景：
1. 新请求：此时，任一单元都可以处理该请求，属于any路由方式。
2. 重试请求：此时，需要和第一次路由保持同样的单元，属于fix路由方式。

## 查单，知道单元编号
属于fix路由方式。

## 查单，不知道单元编号
属于try路由方式。

## 获取订单列表
请求需要聚合所有单元的数据，然后返回。属于any路由方式，接口收到请求之后，自行请求其它单元获取所有数据合并返回。


4. 仅在unit1比例为100%时，路由到unit1，其它情况都路由到unit0.原因是unit1是提供有损服务的。
   1. prefer_unit_id=0 支持。当优先单元0的时候，仅当100%路由到unit1时，才路由unit1.
   2. 设计原因：比例变化不应该导致业务受影响。因此，非比例的需求都不应该通过比例控制来达成。
5. 有些action会终止执行接下来的表达式，有些不会，可以显示通过terminal控制。
6. 通过使用lua提供额外的aciton，可以最大化灵活性。基础功能简单易用（只支持了单一的expr，不支持and or等判断），又有很强的扩展性。
7. 克制的增加特性，但这不是设计简陋的理由。
8. 模板功能可以通过引用预定义变量实现？在规则文件中可以这样做，但是对于一些配置，就不行，比如域名配置。
   1. ${env:cid}, ${env:dot_env}, ${env:domain_suffix}
9. terminal改成了指针形式，这样可以通过配置重新指定true和false的场景，结合action的默认terminal属性，灵活性更高。
10. api路由策略分类：
   1. any：任意单元可以处理请求。
   2. fix：指定单元才可以处理情况。
   3. any结合fix：根据routing-log决定走哪一个。先any定，然后根据routing-log修正。
   4. consult_and_retry：只能一个单元处理，但是预先不知道是哪个单元。如果一个接口既可以是any，也可以是consult_and_retry，应该使用consult_and_retry。
11. prefer_unit_id，尽可能路由到指定单元。可能是另外一个单元仅有损服务。

不同国家的常量、集合值不同，如何处理？是否可以结合模板功能实现？

# 方案设计
单元化的本质是数据的单元化。
## Action
Action封装了一个动作，该动作对输入进行处理，并产生一些输出。

## 表达式
要关注数据类型，重点关注数字、字符串、数组，函数对这些类型的处理可能不符合用户的预期。

## 变量
1. 需要可以引用body的整个内容，而不是取某一个key的值。

## 如何选择单元
有三种方式：
1. 随机数。太灵活了，不好，除非是无状态接口，否则不建议使用。
2. 单号。流量更加均匀，更受控。
3. 用户ID。用户单热点问题，用户可能不是自然人，同一个账号出现大量订单，无发在单元间进行有效分布。
   1. 优点是，系统的缓存一致性会比较好。

结论：有单号选单号，没单号选用户ID，最后选随机数。
