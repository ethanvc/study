## 总览
如下的一些操作涉及加锁：
1. 备份整个数据库。
   1. FLUSH TABLES WITH READ LOCK：
   2. --single-transaction
   3. LOCK BINLOG FOR BACKUP
2. 修改表结构。
3. 像表中插入记录。


意向锁（Intention Lock）： IX和IS锁定的主要目的是表明有人正在锁定行，或者将要锁定表中的行，
这样可以更好、更高效的检测行级锁和表级锁的存在，避免冲突。

当需要获取表琐的时候，需要检查意向锁，避免表琐和行级锁冲突。

当前读。

